{
  "version": 2,
  "functions": {
    "api/*": {
      "rewrites": [{ "source": "/api/(.*)", "destination": "/api/$1" }]
    }
  },
  "builds": [
    {
      "src": "*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
